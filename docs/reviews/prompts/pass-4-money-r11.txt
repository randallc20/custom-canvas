You are reviewing the Custom Canvas codebase — a Houston art marketplace: Next.js 14 App Router, Supabase (Postgres + RLS + storage + auth), Stripe Connect, React Query, Tailwind. ~327 TS/TSX files, ~24k LOC in src/, 48 SQL migrations, Playwright e2e. It is live on production but payments are still gated off pending Stripe activation.

Your job this session is one pass: Money & order lifecycle. Read only the scope below, and produce a written report. Nothing else.

## Why this pass exists

This is the SIXTH money pass and it is the arc's gate. The rule is: repeat
review passes until one returns **no P0 and no P1**. Round r10 was the first
with no P0. It found a P1 (the approval door and the settle door disagreed
about the artist's "buyer never collected" answer), that P1 is fixed, and this
pass decides whether the arc is finished.

So the bar is different from the previous five. Do not go looking for volume.
Go looking for whether the LAST ROUND OF FIXES broke something, because that is
what the previous five rounds each found: every single P0 this arc produced was
produced by a fix, not by the original build.

## Scope — HEAD, and above all `git log -p -3`

Read `git log -p -3` in full first. The three commits are:
- `f875d61` — five fixes: the acceptance gate refusing instead of throwing; the
  artist's Cancel-order button hidden and refused after they approve a refund;
  look-before-you-create on both Stripe money steps; the approval / return /
  requireUnshipped gates skipped on a RESUME; and `refund_reason` written
  unconditionally instead of first-writer-wins.
- `474e094`, `1ebb6b9` — docs only.

Then read all of these at HEAD:

- `src/lib/settleRefund.ts` — read it three times. Four of the five fixes are
  in this file and they interact. The new `moneyHasMoved` flag turns off three
  gates; the new Stripe `list` calls adopt objects the platform did not create;
  the reason write lost its `.is('refund_reason', null)` predicate.
- `src/lib/settleRefund.test.ts` — NEW. The tests claim to pin all of that.
  Are they pinning what they say? A test whose stub swallows the thing under
  test is worse than no test — one in this very file did exactly that before
  the stub was taught to model `.is()`.
- `src/lib/cancelUnshipped.ts`, `src/lib/orderReturns.ts`,
  `src/utils/orderReturns.ts`, `src/utils/refundSplit.ts`,
  `src/utils/fulfillment.ts`, `src/utils/fulfillmentWindow.ts`,
  `src/utils/evaluateProtection.ts`, `src/lib/assessProtection.ts`
- `src/app/api/orders/[id]/cancel-unshipped/route.ts` (changed),
  `.../approve-refund/route.ts`, `.../accept-ship-by/route.ts`,
  `.../return-shipped/route.ts`, `.../propose-ship-by/route.ts`
- `src/app/api/admin/orders/[id]/refund/route.ts`, `.../return/route.ts`,
  `.../signature-confirmed/route.ts`
- `src/app/api/cron/fulfillment-windows/route.ts` — read it twice.
- `src/components/studio/SalesSection.tsx` (changed) and
  `src/app/(user)/orders/page.tsx` — the two order cards, which must agree.
- `src/app/api/webhooks/stripe/route.ts` — `charge.refunded` and the dispute
  handlers only.
- `scripts/db-smoke.sql` §6 and §14.

## The questions I actually want answered

- **`moneyHasMoved` turns off three gates.** It is `!!order.stripe_refund_id`,
  read once at the top. Is that read trustworthy — can it be true when money
  has NOT moved, or false when it has? What does the flag do on a pickup order,
  on a disputed order, on an order whose refund id was adopted from a Dashboard
  refund a human issued for a different reason? Enumerate every gate it now
  skips and say, for each, what a bad actor or an unlucky sequence gets.
- **Adoption.** `stripe.refunds.list({payment_intent})` takes `data[0]` —
  ANY refund, including a partial one an admin issued by hand. The order is then
  closed as fully refunded and possibly relisted. Is that the right trade
  against double-refunding, and what is the worst concrete case? Same question
  for `transfers.listReversals` and a PARTIAL reversal.
- **The unconditional reason write.** The predicate is gone. Two settles racing
  on one order now both write a reason. Can the row end up describing a split
  different from the money that moved? Trace the interleaving.
- **The artist's cancel refusal.** `refund_approved_at` now blocks the artist
  branch of `cancel-unshipped`. Does that strand any order — an approved refund
  the artist must still be able to cancel, a pickup order, an order where the
  buyer's branch is also closed? And is the Studio button's new condition the
  same condition as the route's?
- **The two order cards.** SalesSection now hides the whole L7 block once a
  refund is approved. Does the artist lose any information or action they still
  need — the ship-by date, the proposal state, anything the buyer's card still
  shows and theirs no longer does?
- **Everything the five fixes touched, once more against the possession
  predicates.** Five rounds running, the P0 has been "does the buyer have the
  piece". `buyerTookPossession()`, `pickupPossessionUnknown()` and
  `pieceIsWithArtist()` partition the states. Does the resume path still ask
  the question at all — and should it?

## What is already known and NOT the point of this pass

`docs/POST-LAUNCH-BACKLOG.md` lists seventeen open P2/P3s that are deliberately
deferred. Read it. Do NOT re-report any of them. Raise one only if a fix in
`f875d61` made it worse, and say so explicitly.

Read `docs/reviews/04-money-r5.md` … `-r10.md` and
`docs/reviews/01-auth-access-r3.md` … `-r8.md` by finding heading so you do not
re-report anything else either.

## Ground rules

**Report only.** The one file you may write is `docs/reviews/04-money-r11.md`. Do not fix anything, do not refactor, do not run a codemod. If a fix is one obvious line, describe it in the finding — do not apply it.

**Read before you claim.** A finding is real only if you have read the code that would break *and its callers*. "This doesn't validate X" is not a finding when the caller validates X. Reading a file's imports is not reading the file.

**Try to disprove each finding before you write it.** For every candidate, make one honest attempt at arguing it is NOT a bug — a guard elsewhere, an RLS policy, a type that makes the input impossible, a caller that already handles it. Drop the ones you cannot defend after that attempt. Six findings that are all real beat thirty where I have to work out which six.

**Every finding needs a concrete failure path.** Not "this could be a problem." Say who does what and what goes wrong: "an artist opens another artist's listing edit URL; the fetch filters on id but not owner, so the page renders their listing; the save then returns zero rows at RLS and the UI shows a success toast." If you cannot write that sentence, it is not a finding — it is a nit, and nits go in the appendix.

**Uncertainty is fine; faking certainty is not.** If you suspect something but cannot confirm it without running the app or seeing production data, mark it UNVERIFIED and say exactly what would settle it.

## Already settled — do not raise these

Decisions, not oversights. Flagging them wastes the pass:

- Destination charges with the platform as merchant of record. Not direct charges.
- Stripe Express connected accounts, Stripe-hosted onboarding. Not embedded components.
- The buyer covers Stripe's processing fee as a "Service fee" line. Platform commission is 15% of the artwork price only, never shipping. The artist is paid price − commission + shipping.
- Payments are deliberately gated behind NEXT_PUBLIC_PAYMENTS_ENABLED until live activation. That flag being false is not a bug.
- npm, never pnpm (a stray pnpm-lock.yaml flips Vercel's package manager). Prod deploys are manual and pinned.
- Next.js 14 App Router + Supabase + React Query + Tailwind. Do not propose migrating the framework, the data layer, the state library, or the CSS approach.
- `docs/CONVENTIONS.md` is the house standard, earned from a real defect sweep. Code that follows it is correct by definition. Code that VIOLATES it is a finding worth raising — check for that specifically.

Also out of scope: formatting and naming taste, "add a comment here", dependency bumps, test-file style, and anything you would phrase as "consider extracting".

## Context to read first

`docs/CONVENTIONS.md`, `README.md`, `DECISIONS.md`. Then know this history: a full-platform adversarial review ran 2026-08-25 and all 15 of its defects are fixed; a six-phase hardening arc shipped after it; two rounds of live tester feedback are closed; a real staging purchase plus dispute and payout reversal were verified end to end. Assume the obvious surface bugs are gone. Look where those passes did not.

## Severity

- **P0** — loses money, exposes another user's data, or corrupts state. Launch-blocking.
- **P1** — a real user hits it in a normal flow and the product is visibly broken.
- **P2** — breaks under growth or under an unusual-but-real input; fine at today's volume.
- **P3** — maintainability: the next change in this code is likely to introduce a bug.

Cap the report at 12 findings, ranked, P0 first. More than that and the strongest 12 get full write-ups while the rest get one line each in the appendix. A pass that honestly finds two P0s and nothing else is a good pass. Do not pad to fill the cap.

## Report format

Write `docs/reviews/04-money-r11.md` in this shape:

# Money & order lifecycle — review r11 (legal-alignment arc) — <date>

**Files read:** every file you actually opened. If you skipped part of the scope, say which part and why.
**Verdict:** two sentences on the health of this slice.

Then one section per finding:

### P<n> — <one-line claim>
**Where:** `path/to/file.ts:120-134`
**What happens:** the concrete failure path.
**Why it's real:** what you read that rules out the innocent explanation.
**Fix direction:** two sentences. Not a patch.

Then close with two sections:

## Appendix: minor
One line each.

## Not findings
Things you looked at hard and concluded were fine, one line each. This is how I know what you actually covered.

Start by listing the files in scope and giving me your reading plan. Then read. Then report.
