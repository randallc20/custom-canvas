'use client';

import { ArtistProfileEdit } from '@/components/profile/ArtistProfileEdit';

// "Public Page" — the artist's profile editor, living inside the Studio.
export default function StudioPublicPagePage() {
  return <ArtistProfileEdit />;
}
